# Test map literals and field access via (get [map, key]).
# Creates a map with two fields and retrieves each by name.

main:
  (if [(= [(get [{x: 10, y: 20}, x]), 10]),
    (if [(= [(get [{x: 10, y: 20}, y]), 20]),
      0,
      1]),
    1])
