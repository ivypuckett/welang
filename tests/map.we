# Test map literals and field access via dot notation and (get [map, key]).
# Creates a map with two fields and retrieves each by name.

main:
  {({x: 10, y: 20}.x.equals 10):
    {({x: 10, y: 20}.y.equals 20): 0, _: 1},
  _: 1}
