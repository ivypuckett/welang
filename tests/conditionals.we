# Tests if/else and the rename binding.
abs: (if [(less-than [x, 0]), (subtract [0, x]), x])

positive-only: (if [(greater-than [x, 0]), x])

# rename x to n for readability
abs-named: (n: (if [(less-than [n, 0]), (subtract [0, n]), n]))

main: (if [(equal [(abs -7), 7]), 0, 1])
