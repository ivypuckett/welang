; Tests if/else and the rename binding.
abs: (if [(< [x, 0]), (- [0, x]), x])

positive-only: (if [(> [x, 0]), x])

; rename x to n for readability
abs-named: (n: (if [(< [n, 0]), (- [0, n]), n]))

main: () (abs -7)
