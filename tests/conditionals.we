# Tests if/else and the rename binding.
abs: (if [(lessThan [x, 0]), (subtract [0, x]), x])

positive-only: (if [(greaterThan [x, 0]), x])

# rename x to n for readability
abs-named: (n: (if [(lessThan [n, 0]), (subtract [0, n]), n]))

main: (if [(equal [(abs -7), 7]), 0, 1])
