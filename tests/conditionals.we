# Tests if/else and the rename binding.
abs: (if [(lt [x, 0]), (sub [0, x]), x])

positive-only: (if [(gt [x, 0]), x])

# rename x to n for readability
abs-named: (n: (if [(lt [n, 0]), (sub [0, n]), n]))

main: (if [(eq [(abs -7), 7]), 0, 1])
