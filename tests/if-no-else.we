# Tests that (if [cond, then]) returns 0 when the condition is false
# (no else branch means the implicit else value is 0).
clamp-positive: (if [(> [x, 0]), x])

main:
  (if [(= [(clamp-positive -5), 0]),
    (if [(= [(clamp-positive 3), 3]),
      0,
      1]),
    1])
