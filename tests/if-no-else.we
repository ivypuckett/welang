# Tests that the wildcard `_` arm is taken when no condition matches.
clamp-positive: {(x.greaterThan 0): x, _: 0}

main:
  {((clamp-positive -5).equals 0):
    {((clamp-positive 3).equals 3): 0, _: 1},
  _: 1}
