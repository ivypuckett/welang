# Tests that the wildcard `_` arm is taken when no condition matches.
clamp-positive: {(greaterThan [x, 0]): x, _: 0}

main:
  {(equal [(clamp-positive -5), 0]):
    {(equal [(clamp-positive 3), 3]): 0, _: 1},
  _: 1}
