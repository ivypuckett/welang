# Tests that (name: body) rename binding works correctly at runtime,
# giving the implicit parameter an explicit name inside the body.
abs-named: (n: (if [(lt [n, 0]), (sub [0, n]), n]))

main:
  (if [(eq [(abs-named -9), 9]),
    (if [(eq [(abs-named 5), 5]),
      0,
      1]),
    1])
