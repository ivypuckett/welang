; Tests all six comparison operators: = < > <= >= and booleans.
clamp: (x lo hi)
  (if (< x lo)
    lo
    (if (> x hi)
      hi
      x))

eq: (a b) (= a b)
lte: (a b) (<= a b)
gte: (a b) (>= a b)

main: () (clamp 5 0 10)
