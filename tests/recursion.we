# Tests recursive function calls.
factorial:
  (if [(<= [x, 1]),
    1,
    (* [x, (factorial (- [x, 1]))])])

fib:
  (if [(< [x, 2]),
    x,
    (+ [(fib (- [x, 1])), (fib (- [x, 2]))])])

main: (if [(= [(factorial 5), 120]), 0, 1])
