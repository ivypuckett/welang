# Tests recursive function calls.
factorial:
  (if [(lessThanOrEqual [x, 1]),
    1,
    (multiply [x, (factorial (subtract [x, 1]))])])

fib:
  (if [(lessThan [x, 2]),
    x,
    (add [(fib (subtract [x, 1])), (fib (subtract [x, 2]))])])

main: (if [(equal [(factorial 5), 120]), 0, 1])
