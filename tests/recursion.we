# Tests recursive function calls.
factorial:
  (if [(lte [x, 1]),
    1,
    (mul [x, (factorial (sub [x, 1]))])])

fib:
  (if [(lt [x, 2]),
    x,
    (add [(fib (sub [x, 1])), (fib (sub [x, 2]))])])

main: (if [(eq [(factorial 5), 120]), 0, 1])
