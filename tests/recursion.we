# Tests recursive function calls.
factorial:
  {(lessThanOrEqual [x, 1]):
    1,
  _: (multiply [x, (factorial (subtract [x, 1]))])}

fib:
  {(lessThan [x, 2]):
    x,
  _: (add [(fib (subtract [x, 1])), (fib (subtract [x, 2]))])}

main: {(equal [(factorial 5), 120]): 0, _: 1}
