# Tests that boolean literals can be accepted by a function and returned.
# `true` compiles to 1 and `false` compiles to 0, so the identity function
# returns their integer equivalents unchanged.
identity: x

main:
  {(equal [(identity true), 1]):
    {(equal [(identity false), 0]): 0, _: 1},
  _: 1}
